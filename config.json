{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "JsonUI Config",
    "description": "Build JsonUI options",
    "type": "object",
    "properties": {
        "folder_name": {
            "description": "Specifies the name of the folder where the UI pack will be output.",
            "type": "string"
        },
        "preview": {
            "description": "Install your JsonUI pack to Minecraft preview.",
            "type": "boolean"
        },
        "development": {
            "description": "Install your JsonUI pack to development folder.",
            "type": "boolean"
        },
        "obfuscator_element_name": {
            "description": "Specifies the name of the obfuscator element.",
            "type": "boolean"
        },
        "debug_screen_content": {
            "description": "Add a label to the project to debug which screen content that screen is using.",
            "type": "boolean"
        },
        "manifest": {
            "description": "Contains the manifest information of the UI pack.",
            "type": "object",
            "properties": {
                "name": {
                    "description": "Specifies the name of the UI pack.",
                    "type": "string"
                },
                "version": {
                    "description": "Specifies the version of the UI pack",
                    "type": "array"
                },
                "uuid": {
                    "description": "Specifies the UUID of the UI pack.",
                    "type": "string"
                },
                "description": {
                    "description": "Specifies the description of the UI pack.",
                    "type": "string"
                }
            }
        }
    }
}